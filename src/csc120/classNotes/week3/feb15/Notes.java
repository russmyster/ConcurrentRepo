package csc120.classNotes.week3.feb15;

import java.io.File;
import java.io.FileNotFoundException;

public class Notes {
    /*
    Class constructor:
     - the constructor is a special method, called during instantiation
     - code within constructor specifies how to initialize the object
     - name of the constructor is always the name of the class
     - NO RETURN TYPE
     --> ex. the file construtor for the java defined class creates a file object, w/ info about location of the file
     - File myFile;
     - myFile = new File("C:\MyDocuments\work.txt")
     --> File is the class, myFile is the variable where reference address is stored for File (object)
     --> new is the keyword for File constructor to create the File object,
     ---> the File constructor is a special method in the File class whose purpose is to initialize the File object
     -





     */


    public static void main(String [] args) throws FileNotFoundException{
    //Scanner scnr = new Scanner(System.in);
/*
    File myFile;
    Scanner scan;
    
    myFile = new File( "example.txt" );
    scan = new Scanner( myFile );

    System.out.println( scan.nextLine() );
    System.out.println( scan.next() );
    System.out.println( scan.nextLine() );
    System.out.println( scan.next() );
*/


    /*
     - myString = new String("HIMYM")
     - myString stores the REFERENCE ADDRESS of the String object

     */


     // String s - new String( "Dylan Thomas" );
     // String p = s;
     // System.out.println( s );
     // String q = new String( "Passacalia" );
     // System.out.println( q );
     // q = p;
     // System.out.println( q );
     // s = "Happy Filmore";
     // System.out.println( p );
    }
}
